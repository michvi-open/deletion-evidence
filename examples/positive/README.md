# Positive example

Scenario: `multi_system_deletion-01` (multi_system_deletion)

A fully-COMPLETE Deletion Evidence Record, with a linked Verification / Exception Record confirming it.

Validate it yourself:

```
node -e "console.log(require('../../packages/js/src/index.js').validateDER(require('./der.json')))"
```
